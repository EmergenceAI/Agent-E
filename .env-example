# ==========================
# Agent-E Environment Config
# ==========================

# REQUIRED: Name of the LLM model to use (e.g., gpt-4-turbo)
AUTOGEN_MODEL_NAME=

# REQUIRED: Your API key for the LLM model (e.g., OpenAI API key)
AUTOGEN_MODEL_API_KEY=

# OPTIONAL: Base URL for the model if hosted outside OpenAI (e.g., Azure, Groq, etc.)
# AUTOGEN_MODEL_BASE_URL=https://api.groq.com/openai/v1

# OPTIONAL: Type of model API (e.g., azure for Azure-hosted models)
# AUTOGEN_MODEL_API_TYPE=azure

# OPTIONAL: Model API version (needed for Azure models)
# AUTOGEN_MODEL_API_VERSION=2023-03-15-preview

# OPTIONAL: Temperature for the LLM (controls randomness, default: 0.0 for gpt-* models)
# AUTOGEN_LLM_TEMPERATURE=0.0

# OPTIONAL: Top-p value (controls diversity, default: 0.001 for gpt-* models)
# AUTOGEN_LLM_TOP_P=0.001

# OPTIONAL: Path to local Chrome browser profile (if not using Playwright)
# BROWSER_STORAGE_DIR=/path/to/chrome/profile

# OPTIONAL: Save chat logs to file (true/false, default: true)
SAVE_CHAT_LOGS_TO_FILE=true

# OPTIONAL: Format for logging messages (json/text, default: text)
LOG_MESSAGES_FORMAT=text

# OPTIONAL: Comma-separated list of directories or .py files for additional skills
# ADDITIONAL_SKILL_DIRS=./private_skills,./extra_skills/my_custom_skill.py

# OPTIONAL: Enable planner agent to get user input (true/false, default: false)
# PLANNER_USER_INPUT_SKILL_ENABLED=false

